


.sort((a, b) => b - a) 






비교 함수의 반환 값이 0보다 작을 경우: a를 b보다 앞으로 정렬합니다. (즉, a를 b의 앞에 위치시킵니다.)

비교 함수의 반환 값이 0일 경우: a와 b의 순서를 변경하지 않습니다.즉, 정렬된 상태를 유지합니다.

비교 함수의 반환 값이 0보다 클 경우: a를 b보다 뒤로 정렬합니다. (즉, a를 b의 뒤에 위치시킵니다.)